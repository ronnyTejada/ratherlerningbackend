# ratherlerningbackend

to run locally
git clone > npm install > npm run build > npm run dev or npm start

the connection db is default but you can change it in ./connections

the default user in the db is admin admin
